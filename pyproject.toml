[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "hellolib"
version = "0.1.0"
description = "A simple example Python package that says hello."
authors = [{ name = "Andrii Ruban" }]
readme = "README.md"
requires-python = ">=3.8"